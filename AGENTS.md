# Bluepay 需求 Demo 项目规则

- 本仓库只维护蓝盛代付新需求的正式 HTML Demo。
- 上级产品资料库继续维护 PRD、需求原文、分析、截图、待确认事项和历史资料，不直接纳入本仓库。
- 页面源文件放在 `src/pages/<需求名称>/`。
- 顶部只保留 `后台`、`商户`、`地址池` 三类；多端需求通过 `moduleCodes` 登记到多个端，不建立“跨后台”分类。
- 页面目录只维护在 `src/catalog/project.json`。
- 不直接修改 `index.html` 或 `dist/`。
- 页面有增删改时必须运行 `npm run check`。
- 不写入账号、密码、Token、密钥、真实商户资料、生产接口地址或其他敏感信息。
- 不启动 Obsidian，直接通过文件系统维护项目。
