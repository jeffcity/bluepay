# Bluepay 统一 Demo 项目规则

- 本仓库只维护一个蓝盛代付产品 HTML Demo；新需求持续更新统一入口中的既有业务页面。
- 上级产品资料库继续维护 PRD、需求原文、分析、截图、待确认事项和历史资料，不直接纳入本仓库。
- 页面源文件按业务页面或稳定页面组放在 `src/pages/<业务页面或页面组>/`，不得按需求名称复制平行 Demo。
- 顶部只保留 `后台`、`商户`、`地址池` 三类；多端需求通过 `moduleCodes` 登记到多个端，不建立“跨后台”分类。
- 页面目录只维护在 `src/catalog/project.json`。
- `src/pages/` 下的正式 HTML 页面源必须全部登记到 `src/catalog/project.json`；不得保留未登记的可编辑副本。
- 不直接修改 `index.html` 或 `dist/`。
- 根 `index.html` 是唯一用户评审入口；源码页不是交付入口。完成后通过构建更新统一入口，交付时只给统一入口并列出本次改动页面。
- 更新既有页面时必须保留原有字段、交互和币种能力；只有真实新增产品页面或独立端时才允许新增页面源。
- 页面有增删改时必须运行 `npm run check`。
- 不写入账号、密码、Token、密钥、真实商户资料、生产接口地址或其他敏感信息。
- 不启动 Obsidian，直接通过文件系统维护项目。
