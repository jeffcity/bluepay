# 项目上下文

- 本项目统一维护蓝盛代付后续新需求的 HTML Demo。
- 上级蓝盛代付产品资料库是需求包和原始资料的来源，不直接纳入本仓库。
- 顶部展示只保留 `后台`、`商户`、`地址池` 三类。
- 同时影响多个端的需求只维护一份 Demo，通过页面清单登记到多个端，不建立“跨后台”分类。
- `src/catalog/project.json` 是统一页面目录的唯一清单。
- `index.html` 与 `dist/` 由构建生成，禁止手工编辑。
- 当前已接入 `BP-REQ-001`「商户充值订单统计栏位」、`BP-REQ-002`「机器人分层播报与多人确认」和 `BP-REQ-003`「商户增加资金划转」Demo，影响端为 `ADMIN`；页面清单以 `src/catalog/project.json` 为准。
- GitHub 仓库保持私有；GitHub Pages 仅发布构建产物 `dist/` 供公开评审，发布前需再次检查内容边界。
