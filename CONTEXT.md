# 项目上下文

- 统一入口：顶栏三端（后台 / 商户 / 地址池）+ **产品左侧导航**（不是需求列表）。
- 统一评审入口：仓库根 `index.html`。内部页面源只用于编辑；对用户只给统一入口，再列出本次修改的产品页面，不给 `src/pages/...` 或独立需求 Demo 链接。
- 导航真相源：`src/catalog/project.json` → `nav`。每个叶子是一页：`id/title/doc/boot/req`。
- 更新 Demo = 先定位某业务页对应的唯一源文件，再在该文件上增量更新；既有页面的导航和已实现能力必须保留。只有真正新增产品页面或独立端时才新增源文件；禁止把整包需求挂成外层卡片、复制同一业务页面或用新需求替换旧页面。
- 仓库内所有正式页面源必须登记在 `src/catalog/project.json`；未登记的 HTML 视为遗留副本，不能作为后续编辑入口。
- 壳加载页时注入 `__BP_SURFACE__` / `__BP_BOOT__` / embed 去内页重复侧栏；后台页不得出现在商户端。
- 需求 ID（`BP-REQ-*`）仍在 `pages` 里做追溯，通过 `nav[].req` 反查。
- `index.html` / `dist/` 构建生成，禁手改。页面变更后 `npm run check`。
